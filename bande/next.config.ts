import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // L'indicateur de développement se pose au-dessus de la barre d'onglets et
  // masque le premier onglet sur les captures de vérification.
  devIndicators: false,
};

export default nextConfig;
